import 'package:flutter/material.dart';

class EnvironmentalSensorsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Environmental Sensors Page'));
  }
}
