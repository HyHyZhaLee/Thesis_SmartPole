import 'package:flutter/material.dart';

class EnvironmentalSensorsPage extends StatelessWidget {
  const EnvironmentalSensorsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Environmental Sensors Page'));
  }
}
