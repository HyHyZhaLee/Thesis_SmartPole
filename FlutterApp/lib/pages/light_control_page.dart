import 'package:flutter/material.dart';

class LightControlPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Light Control Page'));
  }
}
